{
  "name": "airwaste-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "AirWaste design system — shared brand tokens + web (Tailwind) and mobile (RN) component libraries.",
  "workspaces": [
    "packages/tokens",
    "packages/web",
    "packages/mobile"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
